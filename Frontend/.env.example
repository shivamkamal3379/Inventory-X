# Base URL the browser uses to reach the API.
#
# Vite inlines this at BUILD time, not runtime — rebuild the image after changing it.
# In the Docker Compose stack, nginx proxies /api to the backend on the same
# origin, so the default is correct and needs no change.
VITE_API_BASE_URL=/api
